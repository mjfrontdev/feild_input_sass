# انواع فیلدها با Sass

پروژه کامل انواع فیلدهای فرم با Sass و بدون استفاده از CDN و رنگ بنفش.

## 📁 ساختار پروژه

```
code2/
├── sass/
│   ├── abstracts/
│   │   ├── _variables.scss    # متغیرها (رنگ‌ها، فونت‌ها، اندازه‌ها)
│   │   └── _mixins.scss       # Mixin ها و توابع قابل استفاده مجدد
│   ├── base/
│   │   ├── _reset.scss        # Reset CSS
│   │   └── _typography.scss   # استایل‌های تایپوگرافی
│   ├── components/
│   │   └── _fields.scss       # استایل‌های تمام فیلدها
│   ├── layout/
│   │   └── _container.scss    # Container و Layout
│   └── main.scss              # فایل اصلی Sass
├── css/
│   └── main.css               # فایل CSS کامپایل شده
├── index.html                 # فایل نمایشی HTML
├── package.json               # وابستگی‌ها و اسکریپت‌ها
└── README.md                  # این فایل
```

## 🚀 نصب و راه‌اندازی

### نصب وابستگی‌ها

```bash
npm install
```

### کامپایل Sass

```bash
# کامپایل یکباره
npm run sass

# کامپایل با نظارت بر تغییرات (watch mode)
npm run sass:watch

# کامپایل فشرده (برای production)
npm run sass:compressed
```

## 📝 انواع فیلدها

### 1. Input Fields
- Text
- Email
- Password
- Number
- Tel
- URL
- Search
- Date, Time, DateTime-local
- سایزها: Small, Medium, Large
- وضعیت‌ها: Normal, Error, Success, Warning, Disabled, Read-only

### 2. Textarea
- سایزهای مختلف
- بدون تغییر اندازه (no-resize)

### 3. Select
- Dropdown استاندارد
- سایزهای مختلف

### 4. Checkbox
- چک‌باکس استاندارد
- گروه Checkbox
- سایزهای مختلف

### 5. Radio Button
- رادیو بوتن استاندارد
- گروه Radio
- سایزهای مختلف

### 6. File Input
- آپلود فایل
- سایزهای مختلف

### 7. Range Input
- اسلایدر محدوده

### 8. Switch (Toggle)
- کلید تغییر وضعیت
- سایزهای مختلف

### 9. Input Group
- فیلد با آیکون یا متن قبل/بعد

## 🎨 رنگ‌بندی

این پروژه از رنگ بنفش استفاده نمی‌کند و از رنگ‌های زیر استفاده می‌کند:

- **Primary (آبی)**: #2563eb
- **Secondary (سبز)**: #10b981
- **Success (سبز)**: #22c55e
- **Warning (نارنجی)**: #f59e0b
- **Danger (قرمز)**: #ef4444
- **Text**: #1f2937
- **Background**: #ffffff, #f9fafb

## 📦 استفاده

بعد از کامپایل Sass، فایل `css/main.css` تولید می‌شود. سپس فایل HTML را در مرورگر باز کنید.

```html
<link rel="stylesheet" href="css/main.css">
```

## 🌙 تم دارک

این پروژه از تم دارک (Dark Mode) پشتیبانی می‌کند!

### ویژگی‌های تم دارک:

- **دکمه تغییر تم**: در گوشه چپ بالای صفحه
- **ذخیره در localStorage**: انتخاب کاربر ذخیره می‌شود
- **تشخیص خودکار**: از تنظیمات سیستم عامل استفاده می‌کند
- **سازگار با تمام فیلدها**: همه انواع فیلدها در تم دارک استایل شده‌اند

### استفاده:

فقط روی دکمه "تم دارک" یا "تم روشن" کلیک کنید!

تم دارک به صورت خودکار برای تمام فیلدها اعمال می‌شود:
- Input fields
- Textarea
- Select
- Checkbox
- Radio
- File Input
- Range
- Switch
- Input Group

## 🔧 سفارشی‌سازی

تمام متغیرها در فایل `sass/abstracts/_variables.scss` قرار دارند و می‌توانید آنها را تغییر دهید:

- رنگ‌ها (Light Mode)
- رنگ‌های تم دارک (Dark Mode)
- اندازه فونت‌ها
- فاصله‌گذاری‌ها (Spacing)
- اندازه فیلدها
- و غیره...

برای تغییر رنگ‌های تم دارک، متغیرهای `$dark-*` را در `sass/abstracts/_variables.scss` ویرایش کنید.

## 📄 لایسنس

MIT

